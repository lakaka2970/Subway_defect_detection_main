# 2026-09-01

## 全量数据盘点 + 阶段报告独立复核

产出：`docs/plans/9.01全量数据盘点/全量数据盘点与可靠结论_20260901.md`
脚本：`scripts/audit_all_data_20260901.py`、`audit_all_data_p2/p3_20260901.py`、
      `verify_rescore_independent_20260901.py`、`verify_significance_20260901.py`、
      `check_leakage_landmines_20260901.py`
产物 JSON：`docs/plans/9.01全量数据盘点/`（audit_all_data / audit_phase2 / audit_phase3 /
          verify_rescore / verify_significance / leakage_report）

### 一、复核结论：8.31/9.01 报告的数字 100% 可信
从 predictions CSV + 基准标签独立重算（未复用 `rescore_loso_offline_20260901.py`）：
- 11 个运行的 TP/FP/FN **逐项一致**
- 8 个逐类召回格子 **逐格一致**
- 6 行等误报率表 **完全一致**
- 879 个误报的类别构成 **完全一致**（CBHPM 508 / CBVPM 362 / 背景类 9）
- 唯一差异是 AP 绝对值（插值口径不同），不影响决策

### 二、三个结论必须修正
1. **r2 在实际运营阈值下不显著**：配对 bootstrap 1000 次，conf=0.10 时
   Δ=+2.1pp、95%CI=[−0.015, +0.056]、P(r2更好)=0.852。只在已决定不用的 conf=0.25 才显著。
2. **不需要逐折标定阈值**：单一全局 t=0.10 与逐折 t\* 等效（r1 −0.3pp / r2 +1.1pp）。
3. **fold3 的失败是 F1B05**（46 GT, Recall 0.065），F1B02 只有 1 个 GT 是噪声。

### 三、🔴 最大发现（含自我修正）
审计中途以为发现了 399 张"带标签未用的检测车原图"，**深挖后证伪**：
- `data/Defect_dataset` 真实构成 = **899 = 500 原图 + 399 张离线增强副本**
  （`_aug0_blur` 66 / `tunnel` 171 / `sunlit` 87 / `weather` 75）
- 顶层 `images/*.jpg` 只有 500 张，是 `images/train|val/` 的字节相同子集 → **此前盘点漏数了一半**
- 399 张副本每张对应 1 个基底，**399 个基底 100% 已在 534 张基准内** → 入训练即泄漏
- 且属已证伪操作（与消融A 同为光照/低照度变体，消融A §13.4 判为确定性负结果）
- 已把 `scripts/build_unused399_manifest_20260901.py` 改写为弃用声明，
  新建 `check_leakage_landmines_20260901.py` 作护栏

### 四、泄漏地雷清单（第 3 轮重训前必须加护栏）
| 源 | 与 534 张基准重叠 |
|---|---|
| `Defect_dataset` 399 增强副本（按基底算） | 399 |
| `train_data_3_raw` | 313 |
| `train_data_3_raw_2560` | 245 |
| `train_data_2`（当前训练集） | 0 ✅ |
| `tiles_normal` 源图 | 0 ✅ |
| `tiles_workshop` 源图 | 0 ✅ |

### 五、真瓶颈 = F1B04（不是"缺数据"）
| 区段 | km 跨度 | 图/km | GT | GT 占比 | Recall@0.10 |
|---|---|---|---|---|---|
| F1B04 | 19.1 km | **15.2** | **204** | **54.3%** | **0.284** |
| F1B03 | 8.0 km | 23.3 | 125 | 33.2% | 0.520 |
| F1B05 | 4.4 km | 12.0 | 46 | 12.2% | 0.065 |
| F1B02 | 1.4 km | 3.6 | 1 | — | — |

- F1B04 提到 F1B03 水平 → 合并召回 0.338 → **0.465**（+12.7pp，直接过 40% 线）
- **F1B04 止于 K53481，F1B05 起于 K54405，相距仅 924 m，Recall 却从 0.284 掉到 0.065**
  → 否定"地理距离/风格漂移"解释，差异必在物理设备形态或安装批次
  → 行动：人工对比两区段各 10 张图，1 小时成本

### 六、账目澄清（疑似问题实为误读）
- 旧7类 CBHPM 534/CBVPM 405 vs 基准 204/172 的"563 框缺口"：是跨集合比较的误读。
  在共同 500 张上：旧7类 338 框 vs 基准 376 框，逐图差值净 +38，
  **与 pass2「恢复 52、删除 14」完全对上**，基准内部自洽。
- Normal ∩ 基准 实测 44（34 张 GT=0 并入 + 10 张来自旧集，其中 4 张带 GT）；
  报告的 69 是对已丢失的 770 集而言，本地可验证的只有 44。
- 旧 7 类标签对真实框覆盖率约 **89.9%**（338→376），所有 Recall 偏保守约 10%。

### 七、其他核实数字
- 重建基准：534 图 / 376 GT / CBHPM 204 + CBVPM 172 / GT=0 图 189 ✅ 与报告一致
- 折几何：f0 F1B04 K34345–45791(146) / f1 F1B04 K45831–53481(144) /
  f2 F1B03 K26305–34259(186) / f3 F1B05 K54405–58825(53) + F1B02 K16446–17836(5)
- 切片：tiles_workshop 44,406 / tiles_workshop_val 3,091 / tiles_real 13,350 / tiles_normal 23,025
- Normal_dataset 1000 张全空标签；区段 F1A04 453 / F1A05 215 / F1B04 155 / F1B05 125 /
  F1A02 26 / F1A03 15 / F1B02 6 / F1B03 5
- **`normal_field_v1`（7846 tile / 679 源图）只在 `data/_upload/normal_extra_tiles.tar` 里，
  从未进任何一轮训练**
- 监督覆盖：目标域可评仅 {5 CBHPM, 6 CBVPM}；仅源域监督 {0-4, 11, 12, 13, 14, 15}；
  全域零样本 {7, 8, 9, 10}

### 八、给出的 ROI 排序（详见报告 §6）
P0：加泄漏护栏 → 落地全局 t=0.10 + 二阶段复核器 → normal_field_v1 入训练 → CBVPM 按类配额
P1：F1B04 加密补采 → **F1B04/F1B05 边界图像对比（1 小时）** → 抽 100 张复核垂直悬吊系旧标签
    → Normal 的 F1A04/F1A05(668 张) 作盲态负样本测试集 → 补采非销钉类
P3：399 增强副本跨折试用（期望低）
不做：消融A 补全 / 像素风格对齐 / GAN 翻译 / PPO
