# 项目长期记忆：地铁接触网缺陷检测

## 数据资产清单（关键，易被遗忘）

| 数据集 | 内容 | 规模 | 成像域 | 当前用途 |
|---|---|---|---|---|
| `data/Defect_dataset` | 检测车实拍，7 类 | **899 = 500 原图 + 399 离线增强副本** / 2482 框 | **目标域** | 500 原图已重建进基准；**399 副本应排除** |
| `data/Defect_dataset_2/Defect_dataset` | 车间静态拍摄，16 类 | 3789 张（配对 3672） | 源域（车间） | **当前唯一训练集** `train_data_2` |
| `data/Normal_dataset` | 检测车实拍**无缺陷**图 | **1000 张，标签全空** | **目标域** | 负样本池（921 张可用） |
| `data/Defect_dataset_16_rebuilt` | 从丢失的 770 集重建 | **534 图 / 376 GT / 仅 2 类** | **目标域** | **唯一外部评测基准** |

**🔴 2026-09-01 修正：`data/Defect_dataset` 的真实构成（此前一直漏数一半）**
- 顶层 `images/*.jpg` 只有 **500 张**，是 `images/train|val/` 共 899 张的**字节相同子集**。
- 899 = **500 原图 + 399 张离线增强副本**（`_aug0_blur` 66 / `tunnel` 171 / `sunlit` 87 / `weather` 75）。
- 399 张副本每张对应 1 个基底，**399 个基底 100% 在 534 张基准内** → 入训练即泄漏；且属已证伪操作（与消融A 同为光照变体）。**不要用。**
- 曾误判为"399 张免费新数据"，已自我修正；`scripts/build_unused399_manifest_20260901.py` 已改为弃用声明。

**关键**：`data/Normal_dataset` 的 921 张（剔除与 770 测试集重叠的 79 张）是免费的目标域负样本与缺陷合成底图，2026-08-29 才被发现，此前 `train_data_2` 的 source 只写了 Defect_dataset_2。
- **`normal_field_v1`（7846 tile / 679 源图）只存在于 `data/_upload/normal_extra_tiles.tar`，至今从未进任何一轮训练** —— 当前唯一立即可用的零成本增量。
- 全部为 5120×5120；1280 tile / stride 960 → **每图 25 个有效位置**。
- 内容过滤（luma_std<8 或 熵<3.0）会丢弃 **~56%** tile（大面积隧道壁无结构）→ 全量有效 tile 约 **1.0 万个**（不是 2.3 万）。
- 先验对齐到 26% 只需 ~5,400 个 tile，即有效 tile 的 ~54%。
- 本地样本集 `data/normal_field_v1_sample/`（19 源图 / 303 tile / 35MB），脚本 `scripts/build_normal_field_sample.py`。

## 类别编号（唯一权威口径 = `data/train_data_2/classes.txt`）

```
0 VHBNM   1 VHBNL   2 SVHBNM  3 SVHBNL  4 SVHTNL  5 CBHPM
6 CBVPM   7 RHTBNM  8 RHTBNL  9 GWCSBNM 10 GWCSBNL
11 GWCNM  12 GWCNL  13 BSBM   14 INSD    15 DRPS
```
- 零样本类：**7 RHTBNM / 8 RHTBNL / 9 GWCSBNM / 10 GWCSBNL**
- ⚠️ `docs/文档/接触网缺陷检测深度学习模型改进计划` 表 2-1 的编号与此**完全不同**（文档写 5=RHTBNM），交付前必须统一
- ⚠️ `subway_defect/models/yolo11s-v2.yaml` 仍写 `nc: 18`

## 项目核心矛盾（2026-08-29 诊断结论）

不是"域泛化能力不足"，而是**数据选型回归**：
1. 8 月重构时把唯一的域内数据（检测车实拍）移出训练集，改用车间单日拍摄数据（`train_data_2` 全部 1039 个拍摄组来自 2026-07-28 09:00–10:59）。
2. `train_data_2` 训练集 **96.1% 的图恰好 1 个标注框，无缺陷图仅 1.3%**（真实线路是 25.8%）→ 模型学到 P(缺陷|部件出现) ≈ 98.7%，这是 2840 个误报的根因。
3. 后果：同域 mAP50 96%，外部 770 张 Recall 0.12%。

## 🔴 泄漏地雷（2026-09-01 全量审计发现，第 3 轮重训前必须加护栏）

| 数据源 | 与 534 张基准重叠 |
|---|---:|
| `data/Defect_dataset` 的 399 张增强副本（按基底算） | **399** |
| `data/train_data_3_raw` | **313** |
| `data/train_data_3_raw_2560` | **245** |
| `data/train_data_2`（当前训练集） | 0 ✅ |
| `tiles_normal` / `tiles_workshop` 源图 | 0 ✅ |

护栏工具：`scripts/check_leakage_landmines_20260901.py`（可 `--scan <目录>`，或 `LeakageGuard().assert_clean()`）。
**注意**：判定重叠时必须剥离 `_augN_*` 后缀再比对基底名，否则增强副本会漏检。

## 已验证为无效的方向（不要再走）

- **离线/在线像素风格对齐**：认真做（直方图匹配+灰度+模糊+噪声）能把 36 个特征弥合率中位数做到 97.3%，但域可分性只从 1.0000 降到 0.9950。实测见 `离线像素域对齐可行性实测_20260829.md`。
- **"域探针 AUC"作为判据**：DG-v1 训练期的激活探针每域只 12 样本，AUC=1.0 是统计必然，无意义。（注：8.28 域差异分析里的**像素级**探针样本 1200 vs 770、特征 36 维，是有效的，两者不要混淆。）
- **GAN/扩散式生成翻译**：会摧毁开口销缺口、螺母间隙这类 5–15 px 细微特征。8.14 已证明 elastic+shadow 温和增强就能让 CBHPM 召回从约 0.9 崩到 0.347。
- **低照度/光照离线变体增强（消融A）**：9.01 判定为**确定性负结果**——fold2 宏AP50 −0.1203，等误报率下差 11.7 倍（FP≈0.36 时 R 0.024 vs 0.280）。不补全 fold1/fold3，方向关闭。**`Defect_dataset` 里那 399 张 `_aug0_*` 副本属同一类操作，同样不要用。**

## 已验证为正确的方向

- 混合域训练（真实线路监督进训练）+ 协议对齐（车间图按 1280 滑窗切片）+ 硬负样本。
- 用文件名里的线路段做**留一区段交叉验证**（F1B04 326 / F1B03 186 / F1A01+A02 96 / F1B05+B01+B02 113）。
- **单一全局置信度阈值 t≈0.10**（9.01 验证）：与逐折标定等效（r1 −0.3pp / r2 +1.1pp），
  不需要建逐区段阈值设施。零训练成本，合并召回 0.157 → 0.32。**但必须配二阶段复核器**
  （FP 1.4~1.7/图 是现场复核工作量）。
- **继续改训练配方的空间已接近噪声水平**：r2 相对 r1 在 t=0.10 处 Δ=+2.1pp，
  配对 bootstrap 95%CI=[−0.015, +0.056]，**不显著**（只在已弃用的 conf=0.25 显著）。

## 🎯 真正的瓶颈 = F1B04（2026-09-01 定量）

| 区段 | km 跨度 | 图/km | GT | GT 占比 | Recall@0.10 |
|---|---|---|---|---|---|
| **F1B04** | 19.1 km | **15.2（最稀疏）** | **204** | **54.3%** | **0.284** |
| F1B03 | 8.0 km | 23.3 | 125 | 33.2% | 0.520 |
| F1B05 | 4.4 km | 12.0 | 46 | 12.2% | 0.065 |
| F1B02 | 1.4 km | 3.6 | 1 | — | —（噪声） |

- **F1B04 提到 F1B03 水平 → 合并召回 0.338 → 0.465（+12.7pp），直接过 40% 线。**
- **F1B04 止于 K53481，F1B05 起于 K54405——相距仅 924 m，Recall 却从 0.284 崩到 0.065。**
  → 否定"地理距离/视觉风格漂移"解释，差异必在**物理设备形态或安装批次**。
  → 待办：人工对比两区段各 10 张图（1 小时），可能直接改写补采策略。
- 补采预算应从"补未见区段"修正为"**加密 F1B04 + 诊断 F1B05**"。
- 折几何：f0 F1B04 K34345–45791(146) / f1 F1B04 K45831–53481(144) /
  f2 F1B03 K26305–34259(186) / f3 F1B05 K54405–58825(53) + F1B02 K16446–17836(5)。

## 已知的系统性偏差

- **旧 7 类标签对真实框的覆盖率约 89.9%**（进基准的 500 张经 pass2 净增 38 框：338→376）。
  所有 Recall 数字**偏保守约 10%**。399 张之外的图无归档 `box_features.csv` 可修正。
- 目标域旧 7 类还有 **2,924 个垂直悬吊系（0–4）框**被判为"误标"整体删除。
  这是唯一剩余的零采集成本目标域监督来源 —— **建议抽 100 张复核**，若 30% 为真即可给 5 个类带来监督。

## 评估纪律（项目级规范）

1. **任何"是否采纳"的决策一律以跨线路验证指标为准**；车间内部 val mAP 只用于监控收敛，不用于选权重、不判定成败。
2. **每轮只改一个变量**，探针 15–20 epoch 定方向，全量只跑确认过的配方。
3. 数据量小时用**留一分组交叉验证**（按物理分组：采集日期/线路段/设备编号），不用随机切分。
4. 最终结论必须留一批**从未参与任何诊断**的真盲测。
5. **合成数据只进训练集，绝不进 val/test**；checkpoint 选择只用真实数据 val。
6. 770 张已参与 DG-v1 诊断，只能当开发集，不能当验收集。

## 商用许可约束

本项目是商业交付。**只用 Apache 2.0 / SAM License 的模型**。
- ✅ SAM 3 / SAM 3.1、Qwen-Image-Edit 系列、FLUX.2-klein-4B
- ❌ FLUX.1 Kontext [dev]（权重非商用）

## 供应商/业主协调

- 场段（车辆段/停车场/试车线）可以设缺拍摄，不载客、不影响运营、天窗期可作业。改进计划 §表 3-2 已列此路径但一直没推进。
- 合同要求 16 类，当前可训练 14 类（缺 9 GWCSBNM / 10 GWCSBNL），需按 §表 3-2 走甲方书面确认。

## 30-40 细分类别扩展：路径决策（2026-09-01 + 09-01 文献检索）

**用户需求**：把 16 大类扩到 30-40 小类（按"接触网器材"细分，如"斜腕臂绝缘子"、"平腕臂绝缘子"、各种线夹）。
**真实数据画像**（滨海快线 1013 条）：61 个接触网器材 × 16 个零部件 × 168 个描述。**头部 30 个器材覆盖 95.3% 缺陷，40 个覆盖 97.7%**——用户的 30-40 估计合理（对应器材层）。

**16 类 = 10 部位 × 6 状态的稀疏子集**（25% 稀疏度）。现有 taxonomy 已是"部位×状态"未显式化的产物。

### ⚠️ 子系统不匹配（最易被忽视）
- **目标域（检测车）拍的是柔性接触网腕臂装配段**（图像已确认：斜腕臂/平腕臂/腕臂底座/绝缘子）
- **车间拍的是刚性接触网模拟段**（汇流排/槽钢/螺母）
- 用户要细分的"斜腕臂/平腕臂绝缘子、线夹"属**柔性系统**——**车间拍不到**
- 16 类中"INSD 绝缘子破损"在目标域 GT = 0（534 张基准里完全没标）

### 推荐：路径 2.5（**单模型多任务头 + 几何/拓扑匹配**）
```
backbone → ┬─ 部件头（16 零部件类，零设缺，5-8万框从 1000 Normal 挖）→ 部件框
           └─ 状态头（10 状态原语，复用 3792 框）                       → 缺陷框
                                          └─ 关系层（IoU + 拓扑 / Transformer cross-attn）
```

**不推荐**：
- 路径 1（端到端 30-40 类）：长尾致命，3 年凑不齐中长尾类样本
- 路径 2 原版（两独立模型）：误差传播、部署 2x、匹配歧义无解

### 模型选型（**不要整体换 Transformer**）
- **状态头留 YOLO11s-v2（CNN）**：3792 框稀缺 + 局部纹理判别 + 8.14 已证模型依赖局部
- **部件头试 RT-DETR / DINO（Transformer）**：5-8 万框充足 + 需要拓扑上下文
- **关系层**：Transformer cross-attention（DETR decoder 天然做查询-键值）
- **备选（性价比最高）**：YOLO + 轻量 Transformer 关系模块（清华博士论文范式）
- **最小验证实验**：1-2 周、2-4 GPU 时，决策门槛=RT-DETR 比 YOLO 召回 ≥+5pp 且 CI 不含 0

### 救场：MVTec-AD 异常检测处理零样本类
- 4 个零样本类（RHTBNM/NL、GWCSBNM/NL）原本只能"按甲方书面确认补采"
- **半监督异常检测**（只用正常样本，识别任意缺陷）→ 把"不可实现"降到"近似可实现"
- 立即可启动（Normal 1000 张里有所有 4 类的"正常态"）

### 路径 2.5 的双重收益
1. 细分类别的基础设施
2. **同时压制误报**：部件头强制学所有正常部件位置和形态，对 879 个误报理论压制率 ≥80%
（CBHPM 误报 508 + CBVPM 误报 362 都是"把正常部件当缺陷"）

### 学术支撑（WebSearch 2026-09-01）
- 清华博士论文 L1（基于 4C 吊弦）：改进 YOLOv7 + 混合域注意力 + 轻量 ViT，**多专家分类器按区域分桶**——路径 2.5 直接对应
- 西南交大硕士 L2（Faster R-CNN + EfficientNet 定位支座）："先定位、后检测"两阶段 9 类 93.48% mAP（**需数据充足**）
- Appl. Sci. 2025 L5：共享编码器 + 双解码器——路径 2.5 架构同构
- ESWA 2026 L7：SCID-Net **"instance-aware inference decoupling"**——业界同向收敛
- MVTec-AD 范式 L9：半监督异常检测，AUROC 98%、44 FPS——零样本类救场

完整评估报告：`docs/plans/9.01全量数据盘点/30-40细分类别扩展_技术路径评估_20260901.md`
