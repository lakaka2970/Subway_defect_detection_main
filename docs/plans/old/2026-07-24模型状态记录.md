# 模型状态记录 — 2026-07-24

## 1. 环境信息

| 项目 | 值 |
|------|-----|
| GPU | NVIDIA GeForce RTX 4090, 24 GB VRAM |
| RAM | 63.8 GB |
| CPU | 24 物理核 / 32 逻辑核 |
| 系统 | Windows (win32) |
| Python | 3.10.20 (Conda env: `Subway`) |
| PyTorch | 2.6.0+cu124 |
| CUDA | 12.4 |
| Ultralytics | 8.4.33 |
| Conda 路径 | `C:\ProgramData\miniconda3` |
| 项目路径 | `C:\Users\Administrator\Subway\Subway_defect_detection_main` |
| Git 分支 | `7.16` |

---

## 2. 模型权重

### 2.1 已训练权重 (`weights/`)

| 文件 | 来源 | 说明 | mAP50 |
|------|------|------|-------|
| `stage1a_public_head.pt` | Stage 1A (40ep, mixed_pretrain) | Head/Neck Warmup, freeze 0-10 | ~0 |
| `stage1b_public_backbone.pt` | Stage 1B (60ep, mixed_pretrain) | Backbone Adapt, freeze 0-5 | 0.312 |
| `stage2_domain_adapt.pt` | Stage 2 (40ep, subway_crops_1024) | 7类域适应, freeze 0-7 | 0.300 |
| `stage3_main.pt` | Stage 3 (76ep, subway_crops 1280px) | **主训练，全解冻** | **0.389** |
| `stage4_finetune.pt` | Stage 4 (7ep, subway_crops 1280px) | 短微调, freeze 0-9 | 0.379 |

**推荐部署模型：`weights/stage3_main.pt`**（mAP50 最高）

### 2.2 模型架构 (`subway_defect/models/`)

| 文件 | 说明 |
|------|------|
| `yolo11s-EMA-SimAM.yaml` | YOLO11s + EMA + SimAM 注意力（推荐） |
| `yolo11m-EMA-SimAM.yaml` | YOLO11m + EMA + SimAM（更高精度） |
| `yolo11m-P2-SimAM.yaml` | YOLO11m + P2 小目标头 + SimAM |

### 2.3 预训练权重 (`yolo_weights/`)

| 文件 | 说明 |
|------|------|
| `yolo11s.pt` | COCO 预训练 YOLO11s（本次训练使用） |

### 2.4 分类器权重（待训练）

| 文件 | 状态 | 说明 |
|------|------|------|
| `weights/classifier_cbhpm.pt` | ❌ 未训练 | CBHPM 二分类 (normal/missing) |
| `weights/classifier_vhbnm_vhbnl.pt` | ❌ 未训练 | VHBNM/VHBNL 四分类 |

---

## 3. 数据集状态

### 3.1 当前数据集（2026-07-24 重新生成）

| 数据集 | Train | Val | 负样本比例 | 总框数 | 用途 |
|--------|-------|-----|-----------|--------|------|
| `data/subway_crops` (1280px) | 14,872 | 2,869 | **55%** | 10,542 | Stage 3/4 主训练 |
| `data/subway_crops_1024` | 11,554 | 2,873 | **71%** | 5,324 | Stage 2 域适应 |
| `data/multi_datasets/mixed_pretrain` | 2,771 | 703 | 85.9% | 787 | Stage 1A/1B 预训练 |

### 3.2 subway_crops 1280px 类别分布

| 类别 | 中文名 | 框数 | 占比 |
|------|--------|------|------|
| VHBNM | 吊弦断裂 | 947 | 9.0% |
| VHBNL | 吊弦松动 | 787 | 7.5% |
| SVHBNM | 斜拉线硬点 | 1,814 | 17.2% |
| SVHBNL | 斜拉线断裂 | 1,149 | 10.9% |
| SVHTNL | 斜拉线脱落 | 655 | 6.2% |
| CBHPM | 接触线硬点 | 1,327 | 12.6% |
| CBVPM | 承力索断股 | 2,170 | 20.6% |
| **总计** | — | **10,542** | 100% |

类别不平衡比：**2.8×**（旧版 3.2×）

### 3.3 生成参数（新版）

```bash
# 1280px（含 multi-scale + copy-paste）
python scripts/generate_native_crops.py --crop-size 1280 \
    --negatives-per-image 10 --balance --minority-multiplier 3 \
    --debiasing --multi-scale --copy-paste

# 1024px
python scripts/generate_native_crops.py --crop-size 1024 \
    --output data/subway_crops_1024 \
    --negatives-per-image 10 --balance --minority-multiplier 3 --debiasing
```

### 3.4 源数据

| 数据 | 路径 | 数量 |
|------|------|------|
| 自制接触网数据 | `data/Defect_dataset/` | 1,116 train / 115 val (12类) |
| KolektorSDD2 | `data/multi_datasets/_downloads/kolektor_sdd2/` | ~3,335 张 |
| RSDDs | `data/multi_datasets/_downloads/rsdds/` | ~195 张 |
| NEU-DET | ❌ 未下载 | 需 Kaggle CLI |
| GC10-DET | ❌ 未下载 | 需 Kaggle CLI |

---

## 4. 训练配置

### 4.1 最优性能参数（RTX 4090 实测）

| 参数 | 1024px | 1280px |
|------|--------|--------|
| batch | 32 | 16 |
| workers | 4 | 4 |
| GPU 利用率 | 93.5% | 88-94% |
| RAM 占用 | 44.7% | 47-52% |
| VRAM | 19.5 GB | 16.6 GB |
| 吞吐量 | 118 samples/s | 66-69 samples/s |

### 4.2 训练阶段配置 (`config/train/pretrain/`)

| 文件 | 阶段 | 关键参数 |
|------|------|----------|
| `stage1a_public_head.yaml` | Head Warmup | SGD lr=0.01, freeze 0-10, 40ep |
| `stage1b_public_backbone.yaml` | Backbone Adapt | AdamW lr=3e-4, freeze 0-5, 60ep |
| `stage2_domain_adapt.yaml` | 域适应 7类 | AdamW lr=8e-4, freeze 0-7, 40ep |
| `stage3_main_training.yaml` | 主训练 | AdamW lr=2e-4, 全解冻, 80ep, patience=28 |
| `stage4_short_finetune.yaml` | 短微调 | AdamW lr=1e-5, freeze 0-9, 10ep, 零增强 |
| `stage5_hard_negative.yaml` | 难负样本 | AdamW lr=5e-5, freeze 0-3, 20ep |

### 4.3 一键训练命令

```bash
python scripts/train_pipeline.py --stages 1a 1b 2 3 4 \
    --model yolo11s-EMA-SimAM --device 0 --workers 4
```

---

## 5. 代码结构

### 5.1 核心模块

```
subway_defect/
├── classes.py              # 16类完整注册表 + 7类训练子集
├── pipeline/
│   ├── two_stage.py        # 二阶段推理管线（ROI + 缺陷检测）
│   ├── slicer.py           # SmartSlicer 智能切片
│   ├── wbf_fusion.py       # WBF 双模型融合
│   └── utils.py            # box_iou, load_class_thresholds
├── classifier/             # ★ 新增：二阶段状态分类器
│   ├── model.py            # MobileNetV3-small (1.07M params)
│   ├── dataset.py          # 分类器数据集 + 加权采样
│   ├── train.py            # 训练逻辑 (CosineAnnealing + EarlyStopping)
│   └── inference.py        # ClassifierReasoner (StateReasoner 协议)
├── augmentations/
│   ├── scene.py            # 6种场景增强函数
│   ├── defect_copy_paste.py # 离线 Copy-Paste 增强
│   └── contactnet_copy_paste.py # 在线 Copy-Paste (Ultralytics)
├── train/
│   ├── configs.py          # HardwareProfile + 训练预设
│   ├── train_defect.py     # 缺陷检测器训练
│   └── train_roi.py        # ROI 提议器训练
└── deployment/
    └── fastapi_server.py   # FastAPI 推理服务
```

### 5.2 脚本工具

```
scripts/
├── generate_native_crops.py      # ★ 已修改：neg=10, multi-scale, copy-paste, minority×3
├── multi_source_dataset_builder.py # ★ 已修改：Kaggle token 自动加载
├── mix_augmented_data.py         # ★ 新增：混入增强数据 + 源级去重
├── prepare_classifier_data.py    # ★ 新增：提取 proposal → 状态标签
├── train_state_classifier.py     # ★ 新增：训练状态分类器
├── perf_experiment.py            # ★ 新增：性能实验脚本
├── train_pipeline.py             # 一键多阶段训练
├── generate_scene_augmentations.py # 场景增强
├── generate_defect_copy_paste.py # Copy-Paste 增强
├── collect_hard_negatives.py     # 难负样本挖掘
├── calibrate_thresholds.py       # 每类阈值校准
├── validate_dataset.py           # 数据集校验
├── split_dataset.py              # 源级 train/val 分割
└── benchmark_throughput.py       # 吞吐量基准测试
```

### 5.3 监控模块

```
subway_yolo/utils/callbacks/perf_monitor.py  # ★ 新增：训练性能监控回调
```

---

## 6. 训练结果（2026-07-24 完成）

### 6.1 Stage 3 最终验证结果（best.pt, mAP50=0.389）

| 类别 | Precision | Recall | mAP50 | mAP50-95 |
|------|-----------|--------|-------|----------|
| CBVPM | 0.398 | 0.773 | **0.753** | 0.426 |
| CBHPM | 0.456 | 0.722 | **0.694** | 0.346 |
| VHBNM | 0.587 | 0.423 | 0.458 | 0.294 |
| VHBNL | 0.273 | 0.333 | 0.319 | 0.215 |
| SVHBNM | 0.149 | 0.620 | 0.176 | 0.096 |
| SVHBNL | 0.135 | 0.476 | 0.173 | 0.094 |
| SVHTNL | 0.119 | 0.667 | 0.146 | 0.082 |
| **all** | **0.302** | **0.573** | **0.389** | **0.222** |

### 6.2 训练耗时

| 阶段 | 耗时 |
|------|------|
| Stage 1A | 18.4 min |
| Stage 1B | 24.1 min |
| Stage 2 | 1.54 hr |
| Stage 3 | ~4.5 hr |
| Stage 4 | ~18 min |
| **总计** | **~7 hr** |

---

## 7. 待完成事项

### 7.1 高优先级

| 事项 | 命令 | 预期效果 |
|------|------|----------|
| 准备分类器数据 | `python scripts/prepare_classifier_data.py` | 生成 `data/classifier/` |
| 训练 CBHPM 分类器 | `python scripts/train_state_classifier.py --task cbhpm` | FP 降低 ≥30% |
| 训练 VHBNM/VHBNL 分类器 | `python scripts/train_state_classifier.py --task vhbnm_vhbnl` | 状态判别 |
| 用新数据集重新训练 | `python scripts/train_pipeline.py --stages 1a 1b 2 3 4 --model yolo11s-EMA-SimAM` | mAP50 > 0.40 |

### 7.2 中优先级

| 事项 | 说明 |
|------|------|
| 下载 NEU-DET + GC10-DET | Kaggle token 已配置在 `docs/kaggle_token.txt` |
| 重建 mixed_pretrain | 补充公开数据后正样本从 598 → ~5000+ |
| Stage 5 难负样本挖掘 | 用 Stage 3 模型推理 → 收集 FP → 微调 |
| 每类阈值校准 | `python scripts/calibrate_thresholds.py` |

### 7.3 低优先级

| 事项 | 说明 |
|------|------|
| P2 小目标检测头 | 对 VHBNL (0.44% 面积) 可能有帮助 |
| 多尺度 Crop 策略 | 640px crop 已集成到 generate_native_crops.py |
| Focal Loss 调参 | fl_gamma 从 2.0 提高到 3.0-5.0 |

---

## 8. 已知问题

| 问题 | 影响 | 状态 |
|------|------|------|
| SVHBNM/SVHTNL 大框低 mAP | 可能存在标注质量问题 | 待审查 |
| mixed_pretrain 正样本仅 598 | 预训练效果有限 | 待补充 NEU-DET/GC10-DET |
| Stage 4 微调未提升 (0.379 < 0.389) | 短微调无增益 | 使用 Stage 3 权重 |
| RAM 偶尔触及 52% (1280px) | 接近 50% 目标上限 | 可接受 |
| 分类器未训练 | 二阶段验证不可用 | 待执行 |

---

## 9. 快速恢复指南

### 环境激活

```bash
C:\ProgramData\miniconda3\Scripts\activate.bat Subway
cd C:\Users\Administrator\Subway\Subway_defect_detection_main
```

### 验证环境

```bash
python -c "import torch; print(torch.cuda.get_device_name(0))"
python -c "from subway_defect.classifier import StateClassifier; print('OK')"
python -c "from subway_defect.pipeline import TwoStagePipeline; print('OK')"
```

### 重新生成数据集

```bash
# 删除旧数据
rmdir /s /q data\subway_crops data\subway_crops_1024

# 重新生成
python scripts/generate_native_crops.py --crop-size 1280 --negatives-per-image 10 --balance --minority-multiplier 3 --debiasing --multi-scale --copy-paste
python scripts/generate_native_crops.py --crop-size 1024 --output data/subway_crops_1024 --negatives-per-image 10 --balance --minority-multiplier 3 --debiasing
```

### 完整训练流程

```bash
python scripts/train_pipeline.py --stages 1a 1b 2 3 4 --model yolo11s-EMA-SimAM --device 0 --workers 4
```

### 分类器训练流程

```bash
python scripts/prepare_classifier_data.py --model weights/stage3_main.pt
python scripts/train_state_classifier.py --task cbhpm --evaluate
python scripts/train_state_classifier.py --task vhbnm_vhbnl --evaluate
```

---

*记录时间：2026-07-24*
*记录人：AI 训练助手*
